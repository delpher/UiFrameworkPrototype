<Application x:Class="UiFramework.SampleApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:elements="clr-namespace:UiFramework.Elements;assembly=UiFramework"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <DataTemplate DataType="{x:Type elements:TextViewModel}">
            <TextBlock Text="{Binding Text}"></TextBlock>
        </DataTemplate>
        <DataTemplate DataType="{x:Type elements:ContainerViewModel}">
            <ItemsControl ItemsSource="{Binding Children}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding}"></ContentPresenter>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </DataTemplate>
        <DataTemplate DataType="{x:Type elements:ButtonViewModel}">
            <Button Content="{Binding Text}" Command="{Binding Click}"/>
        </DataTemplate>
    </Application.Resources>
</Application>
